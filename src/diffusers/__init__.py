"""Stand-in package root for first contributions.

This kit is not a full `huggingface/diffusers` checkout. Scaffolded schedulers
land in `src/diffusers/schedulers/` so the path matches the real library.
Runtime imports still expect the `diffusers` package to be installed.
"""
